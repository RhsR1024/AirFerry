<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Receive needs a camera, but the sender mode must stay installable on
         camera-less devices; ScanActivity still requests CAMERA at runtime. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".AirFerryApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirFerry">

        <activity
            android:name=".ui.ScanActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SendActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.ReceiveDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity" />

        <activity
            android:name=".ui.ReceiveTextActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity" />

        <activity
            android:name=".ui.ReceiveBundleActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity" />

        <activity
            android:name=".ui.FileListActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.ScanActivity" />

        <!-- FileProvider for sharing received files via ACTION_SEND -->
        <provider
            android:name=".scan.AirFerryFileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
